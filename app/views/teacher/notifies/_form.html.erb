<%= form_for([:teacher, @teacher_notify]) do |f| %>
  <% if @teacher_notify.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_notify.errors.count, "error") %> prohibited this teacher_notify from being saved:</h2>

      <ul>
      <% @teacher_notify.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
