<% @page_title=@course.name+"课程管理"  -%>
<div class="title">修改通知</div>
<div class="content">
<%= render 'form' %>
</div>
<%= link_to 'Show', [:teacher, @course, @notify] %> |
<%= link_to 'Back', teacher_course_notifies_path(@course) %>
