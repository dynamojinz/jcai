<%= form_for([:teacher, @teacher_message]) do |f| %>
  <% if @teacher_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_message.errors.count, "error") %> prohibited this teacher_message from being saved:</h2>

      <ul>
      <% @teacher_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
