<%= form_for([:teacher, @course, @message]) do |f| %>
  <% if @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this teacher_message from being saved:</h2>

      <ul>
      <% @message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div id="message">
  <div class="mtitle"><%= h(@message.student.name) if @message.student %>同学发表于<%=h datetime_string(@message.created_at) %></div>
  <div class="mcontent"><%= simple_format(h(@message.question)) %></div>
  <div class="mtitle"><label for="message_question">回复留言：</label></div>
  <div class="mcontent"><%= f.text_area 'reply', :cols => 70, :rows => 12  %></p></div>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
