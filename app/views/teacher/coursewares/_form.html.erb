<%= form_for([:teacher, @course, @courseware]) do |f| %>
  <% if @courseware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courseware.errors.count, "error") %> prohibited this teacher_courseware from being saved:</h2>

      <ul>
      <% @courseware.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
	<%= f.text_area :description, :cols => 50, :rows => 10 %>
  </div>
  <div class="field">
    <%= f.label :courseware %><br />
    <%= f.file_field :courseware %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
