<% @page_title=@course.name+"课程管理"  -%>
<div class="title"><%= @exam.name %>－－试题管理</div>
<%= render(:partial => 'question', 
                 :collection => @questions,
                 :spacer => "<br />")
%>
<%= paginate @questions %>
<br />

<%= link_to '添加试题', new_teacher_course_exam_question_path(@course, @exam) %> | 
<%= link_to '返回', teacher_course_exams_path(@course) %>
