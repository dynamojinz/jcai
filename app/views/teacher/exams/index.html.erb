<h1>Listing teacher_exams</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teacher_exams.each do |teacher_exam| %>
  <tr>
    <td><%= link_to 'Show', teacher_exam %></td>
    <td><%= link_to 'Edit', edit_teacher_exam_path(teacher_exam) %></td>
    <td><%= link_to 'Destroy', teacher_exam, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Exam', new_teacher_exam_path %>
