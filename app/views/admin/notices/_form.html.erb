<%= form_for([:admin, @admin_notice]) do |f| %>
  <% if @admin_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_notice.errors.count, "error") %> prohibited this admin_notice from being saved:</h2>

      <ul>
      <% @admin_notice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
