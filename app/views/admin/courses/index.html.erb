<h1>Listing admin_courses</h1>

<table>
  <tr>
	<th>Name</th>
	<th>Teacher</th>
	<th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_courses.each do |admin_course| %>
  <tr>
	<td><%= admin_course.name %></td>
	<td><%= admin_course.teacher.name %></td>
	<td><%= admin_course.description %></td>
    <td><%= link_to 'Show', [:admin, admin_course] %></td>
    <td><%= link_to 'Edit', edit_admin_course_path(admin_course) %></td>
    <td><%= link_to 'Destroy', [:admin, admin_course], :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Course', new_admin_course_path %>
