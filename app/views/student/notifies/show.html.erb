<% @page_title= "欢迎你进入#{@course.name}课程学习" -%>

<div class="title"><%= h @course.name %>课程－－查看通知</div>
<p>标题：<%= h @notify.title %></p>
<p>创建时间：<%= h @notify.created_at %>分钟</p>
<p>最后更新：<%= h @notify.updated_at %></p>
<p>内容：</br>
<%= simple_format(h(@notify.content)) %>
</p>


<%= link_to '返回', student_course_notifies_path(@course) %>
